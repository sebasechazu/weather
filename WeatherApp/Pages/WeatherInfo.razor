@using WeatherApp.Models;

@if (weatherData != null)
{
    <div class="card" style="width: 18rem;">
        <h3>@weatherData.Name</h3>
        <p>Temperatura: @weatherData.Temperature °C</p>
        <p>Humedad: @weatherData.Humidity %</p>
        <p>Descripción: @weatherData.Description</p>
        <i class="wi @GetWeatherIconClass(weatherData.Description)"></i>

    </div>
}
@code {
    [Parameter]
    public WeatherData? weatherData { get; set; }
 private string GetWeatherIconClass(string description)
    {
        switch (description.ToLower())
        {
            case "cielo claro":
                return "wi-day-sunny";
            case "pocas nubes":
                return "wi-cloud";
            case "nubes":
                return "wi-cloudy";  
            case "lluvia":
                return "wi-rain";           
            default:
                return "wi-meteor";
        }
    }
}

